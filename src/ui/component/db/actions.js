const Actions = {
    set_current_position
}

/**
 * Triggered when the Board has a new move.
 * @todo Trigger when clicking on a PGN move, maybe already done...
 * IF the setting "lock search position" is enabled, then fetch games
 * that feature the current position displayed on the board.
 * @param {String} fen
 */
function set_current_position(fen) {

}

export default Actions